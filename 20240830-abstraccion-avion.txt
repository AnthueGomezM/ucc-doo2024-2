entradas y salidas
la carga(personas u objetos)
mantenimiento

-Atributos:
marca: cadena de texto que representa la marca del avión (por ejemplo, "Boeing", "Airbus", etc.)
modelo: cadena de texto que representa el modelo del avión (por ejemplo, "737", "A320", etc.)
capacidad: número entero que representa la cantidad de pasajeros que puede transportar el avión
velocidad: número decimal que representa la velocidad máxima del avión en km/h
altitud: número que representa la altitud máxima del avión en metros
tipo
personal
-Métodos:
despegar(): método que simula el despegue del avión
aterrizar(): método que simula el aterrizaje del avión
volar(destino): método que simula el vuelo del avión hacia un destino determinado
protocoloSeguridad():

